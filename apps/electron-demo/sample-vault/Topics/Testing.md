# Testing

Back to [[index]].

Strategy for [[Projects/Nexus-Editor]]:

- Unit tests live under `packages/core/test/`.
- Integration fixtures (this vault) live under
  `apps/electron-demo/sample-vault/`.
- Regression invariants are tracked in `CLAUDE.md`.
